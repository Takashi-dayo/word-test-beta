# みんなの単語帳 v8.1（問題画面の省スペース化）

## 修正内容

- 「完全一致で判定する」チェック欄を削除し、従来の既定値と同じ通常判定へ固定。
- 「日本語訳を入力」「英語を入力」の案内を非表示にして、問題文を上へ移動。
- 発音ボタンを小型スピーカーアイコンへ変更し、英単語の右隣へ配置。
- 英語の模範解答に表示される発音ボタンも、単語の右隣へ配置。
- 不正解確定時は問題カードの外枠と発光を赤色へ変更。
- IndexedDB内の登録単語、学習記録、設定データの仕様は変更していない。

## データ保護

IndexedDB名 `word-study-app-db`、DBバージョン `1`、ストア名 `appState`、レコードキー `main`、アプリデータバージョン `4` は変更していない。
登録単語・学習記録・設定データを削除または初期化する処理は追加していない。

## 反映手順

1. ZIP内の全ファイルをGitHub Pagesリポジトリ直下へ上書きする。
2. 公開ページを完全に閉じ、再度開く。
3. 反映されない場合のみ、ページを再読み込みする。
4. ブラウザのサイトデータは削除しない。
