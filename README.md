# みんなの単語帳 v7.3（データ読込修正版）

## 修正内容

- v7.2に誤って同梱されていた旧版app.jsを、現在のUIと互換性のあるapp.jsへ差し替え。
- 起動時のJavaScript停止を解消し、既存のIndexedDBデータを正常に読み込む。
- Service Workerのキャッシュをv7.3へ更新。
- Android用maskableアイコンはv7.2のまま維持。
- IndexedDB名、ストア名、レコードキー、データバージョンは変更していない。

## 反映手順

1. ZIP内の全ファイルとiconsフォルダを、同じGitHub Pagesリポジトリ直下へ上書きする。
2. 公開ページを完全に閉じ、再度開いて再読み込みする。
3. ブラウザのサイトデータは削除しない。

既存データがIndexedDBに残っていれば、v7.3で再表示される。
