# みんなの単語帳 v7.9（iPhone純正12キー・予測変換抑制）

## 修正内容

- 英語回答時の見た目は、従来どおり1文字ずつの枠に分ける。
- 実入力には非表示の安全入力欄を使用し、iPhone純正12キーの同一キー連打を正しく1文字として処理する。
- 英語回答時は安全入力欄として扱い、予測変換・自動修正・スペルチェックを抑制する。
- 日本語回答時の入力方式は変更していない。

## データ保護

IndexedDB名 `word-study-app-db`、DBバージョン `1`、ストア名 `appState`、レコードキー `main`、アプリデータバージョン `4` は変更していない。
登録単語・学習記録・設定データを削除または初期化する処理は追加していない。

## 反映手順

1. ZIP内の全ファイルをGitHub Pagesリポジトリ直下へ上書きする。
2. 公開ページを完全に閉じ、再度開く。
3. 反映されない場合のみ、ページを再読み込みする。
4. ブラウザのサイトデータは削除しない。
