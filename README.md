# みんなの単語帳 v7.8（iPhone純正12キー入力修正）

## 修正内容

- iPhone純正キーボードの英字12キー入力で、同じ文字が重複して入力される問題を修正。
- 見た目は従来どおり、英語回答を1文字ずつの枠で表示する。
- 実際の入力先を1つの入力欄に統一し、12キーの連続タップによる文字選択を妨げない方式へ変更。
- フォレスタモードの14級〜1段追加内容は維持。

## データ保護

IndexedDB名 `word-study-app-db`、ストア名 `appState`、レコードキー `main`、データバージョンは変更していない。登録済み単語・学習記録には触れない。

## 反映手順

1. ZIP内の全ファイルをGitHub Pagesリポジトリへ上書きする。
2. `icons` フォルダも同梱されているが、画像自体は変更していない。
3. 公開ページを完全に閉じ、再度開いて再読み込みする。
4. ブラウザのサイトデータは削除しない。
